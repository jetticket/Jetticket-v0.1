# clone the empty repo
git clone https://github.com/jetticket/JetTicket_v1.0.git
cd JetTicket_v1.0

# copy in the folders we planned (frontend, backend, setup, etc.)
# or paste the code I printed above

# then push it
git add .
git commit -m "Initial commit: JetTicket v1.0 AutoDeploy"
git push origin main